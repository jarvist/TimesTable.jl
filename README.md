# TimesTable.jl

Generates a set of randomised times-table questions of given factors (f), but
stochastically includes the commutative pair (i.e. c × f = f × c) and the
'division fact' (c × f = a, then ask a ÷ f) in the next few questions.

